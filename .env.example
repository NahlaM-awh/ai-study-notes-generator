# Optional — no API keys required for default mode

# AI provider: local (default) | ollama | auto
AI_PROVIDER=local

# Optional Ollama (free local LLM) — install from https://ollama.com
OLLAMA_HOST=http://127.0.0.1:11434
OLLAMA_MODEL=llama3.2
